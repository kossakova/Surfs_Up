select rental_duration,min(length) as "h", max (rating) as "b"
from film
group by rental_duration

select avg(film.rental_rate)
from film

select rating,avg(rental_rate)
from film
group by rating 

select rating,sum(rental_rate)
from film
group by rating 

select sum(rental_rate)
from film

select min(film.length), max(film.length)
from film 

select rental_rate,avg(length) as avglen
from film 
group by rental_rate
order by avglen 

select rental_rate,ROUND(avg(length),2) as avglen
from film 
group by rental_rate
order by avglen 

select rental_rate,avg(length) as avglen
from film 
group by rental_rate
order by avglen 
limit 1

select count(first_name)
from actor 

select first_name,COUNT(*)
from actor
group by first_name
order by COUNT(*) desc

--or 

select first_name,COUNT(*) count_ofname
from actor
group by first_name
order by count_ofname desc

select rating,avg(rental_duration)
from film
group by rating
--order by  desc

--to round it

select length,ROUND(avg(replacement_cost),2) avgcost 
from film
group by length
order by length desc
limit 10

select * from city limit 5

select country,count(city)
from city
inner join country 
on city.country_id = country.country_id
group by country 
order by count desc

select distinct on (customer_id)customer_id, rental_date
from rental
order by customer_id, rental_date desc
